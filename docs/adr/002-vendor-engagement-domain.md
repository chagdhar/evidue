# ADR 002: Vendor engagement is the recurring commercial aggregate

**Status:** Accepted

Contracts and invoices are linked to a `VendorEngagement`, not treated as a single active pair. This supports monthly billing history, multiple counterparties, reporting and disputes without changing kernel identifiers.
