# ADR 005: Disputes are stateful cases

**Status:** Accepted

CSV/email exports are outputs, not the dispute system of record. A dispute case is created only from an approved settlement and progresses through explicit lifecycle transitions while retaining item-level provenance.
